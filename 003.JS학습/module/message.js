// const message = (name,age) => {
//     this.name = name;
//     this.age = age;
//     return name + ' is ' + age + 'years old.';
//     };

const message = (name,age) => 
name + ' is ' + age + 'years old.';
    
    export default message;